#include <stdio.h>
/**
 * main - Entry point
 *
 *  Return: Always 0 (Success)
 */
int main(void)
{
	if (n == 0)
		printf("%d is zero", n);
	else if (n > 0)
		printf("%d is positive", n);
	else
		printf("%d is negative", n);

	return (0);
}
