#include <ctype.h>
#include "main.h"

/**
 * int _islower - checks if a letter is lower
 * return: returns 1 if the letter is lower and 0 if it is not
 */
int _islower(int c)
{
	if (islower(c)
	{
		return (1);
	}

	return (0);
}
